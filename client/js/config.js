/**
 * Game configuration.
 * SERVER_URL — change to your Render URL when deploying.
 * MAP_WIDTH/HEIGHT must match server/server.js.
 */
window.GAME_CONFIG = {
  SERVER_URL: 'http://localhost:3000',

  MAP_WIDTH: 1200,
  MAP_HEIGHT: 900,
  PLAYER_SPEED: 220,
  SCAN_RADIUS: 140,
  SCAN_COOLDOWN_MS: 2500
};
